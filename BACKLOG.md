# BACKLOG（待办池）

本文件登记代码审查确认、但**未修复**的非阻断项。

最近更新：2026-09-12。第 2 轮审查确认的 22 条已在本轮清掉 21 条（CLI 与核心、四个算子、交互层、服务端与前端、批处理脚本五路并发修复，见 `39d3a0a` 之后的提交）；余下 1 条属设计取舍，登记备查。
2026-09-14：trim residue 评审新增 2 条（内存取舍 + 防御分支覆盖说明），见表末两行。

## 暂不修（设计取舍）

| 文件:行                             | 问题                                                                                                                                                                                    | 影响                                                                                                                | 处置                                                                                                                                                                         |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/shared/output-naming.ts:10-19` | O_EXCL 占位设计在进程被 Ctrl-C/崩溃时会残留 0 字节占位文件                                                                                                                              | 下次转换会占掉一个编号（目录出现跳号）并留下 0 字节残留文件；不会覆盖或误删文件，危害有限                           | 维持现状。加「启动时清理自家 0 字节占位」的兜底需先能可靠区分自家占位与用户文件，误删用户文件的风险高于跳号本身，故不做                                                      |
| `src/convert.ts:149-152`            | `--interactive` 无路径时先命中 `args.length === 0` 早退（打印「请拖拽图片或包含图片的文件夹到此脚本上运行」），交互菜单不可达；`run_interactive.bat` 双击（命令行不含路径）正落在此分支 | 双击该 bat 不会进入菜单，拖拽图片到 bat 上则一切正常（该脚本的设计用法）                                            | 已评估，有意保持。改成 `args.length === 0 && !isInteractive` 只会让用户走完整个菜单后撞上 `未找到任何受支持的图片文件`（exit 1），体验更差；现状的早退提示准确说明了正确用法 |
| `src/trim.ts:73-122`                | residue 分析多一次全图 raw 解码（整图缓冲常驻），大图批量时内存峰值翻倍                                                                                                                 | 常规图片无感；超大图批量可能 OOM                                                                                    | 维持现状。条带像素少但缓冲是整图的；真遇 OOM 再改按边 extract 后分别 raw                                                                                                     |
| `src/trim.ts:32-70`                 | `feathered`/`content` 分类分支无单测覆盖                                                                                                                                                | 防御分支：羽化/渐变/噪点三套夹具实测 probe 切除带恒自洽（只走 uniform/noisy）；分支撒谎只影响建议可信度，不影响落盘 | 维持现状。硬造触发夹具等于 pin 偶然行为；sharp 大版本变更时重验                                                                                                              |
